# Script Net Health — Release {VERSION}

CI gerou uma nova versao do copiloto NetOps (diagnostico BOTTOM-UP no host Windows).

### Detalhes
- **Versao:** `{VERSION}`
- **Data:** `{DATE}`
- **Config:** `.env` / `.env.example` (`SNH_*`)

### Historico
{CHANGELOG}

---
Release automatico (semantic-release).
